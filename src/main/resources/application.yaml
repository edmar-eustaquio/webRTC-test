server:
  port: ${PORT:8080}
#  address: 192.168.100.99

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/fishbook
    username: root
    password:
      
  jpa:
    hibernate:
      ddl-auto: update
    database: mysql